[package]
name = "parser"
version = "0.1.0"
edition = "2024"

[dependencies]
common = {workspace = true}
# For checking properties of unicode characters
icu_properties = "2.0"
log = "0.4.28"

[features]
test-utils = []